# Review-Fix Rollout — 2026-08-05

Branch `worktree-review-fixes`, based on cleanup-alpha HEAD (40a9e0a), 17 commits.
Applies the confirmed-bug fixes from `docs/review/wave1` + `docs/review/wave2`.
Plan: `docs/superpowers/plans/2026-08-05-review-fixes.md`.

**Nothing here is runtime-verified.** Every fix is a static edit checked against the review's
traced failure scenario. DromEd verification (`script_reload`, `script_test`) is still required,
in particular for the four riskiest changes marked ⚠ below.

## Fixed (by commit)

| Commit | Scope | Tracked IDs closed / advanced |
|---|---|---|
| e346875 | DScript namespace helpers | T-22, T-23, T-25 |
| 9f47b50 | QVar library (Set/Delete/_GetQVarType) | T-34 |
| a2bfd3f | DCheckString operators | T-11, T-12, T-19, T-20, T-24, T-26, T-67 |
| 9c28ae0 | DBaseTrap staging ⚠ (T-37 runtime init, T-70 per-frame Off) | T-10, T-21, T-37, T-38, T-65, T-70 |
| 7b9d5a4 | DTestTrap guards, PostMessage default, ⚠ DTrigger T-mode timing rework | — |
| 47878c4 | DScriptHandler registries, ⚠ PerFrame restart after load, PerMidFrame freeze | T-33, T-36, T-39 |
| 52652a7 | DHub mechanical fixes (T-40 rewrite items untouched) | T-13, T-14 |
| 499306d | QVar trap classes (DTrigQVar subscribes for the first time) | T-30 |
| 3275138 | SafeDevice / DStdButton / DHitScanTrap | T-15 |
| 3eb95f6 | Utility traps | T-47, T-48 |
| d2419a0 | DRay / DArmAttachment / DObjectPanTo / DDirector | T-35 |
| e4b0580 | DHudObject / DHudCompass | — |
| df0454f | Inventory classes / DRenameItem | T-91 (one manifestation) |
| c8f3d6f | DTweqDevice / DDrunkPlayerTrap / DPortal | — |
| 43de2e6 | Overlays | T-52 |
| 46b4b04 | dfile / dblob / dCSV | — |
| feeb42a | ⚠ Persistence chain (all four fatal links) | — |
| (2026-08-13) | Follow-up pass: undercover suite + teleport/SFX cluster | T-16, T-18, T-31, T-32, T-41, T-42, T-43, T-44, T-45, T-46, T-49 (+1 new: `DImUndercover.DoOff` read hard-coded `"DNotSuspAIUseMetas"` instead of `_script + "UseMetas"`) |

## Intentionally NOT touched

- **Debug prints & dead code** (T-60/T-61/T-62/T-63, commented-out blocks) — owned by the
  other cleanup agent on another branch. Where a structural fix necessarily replaced a print
  (e.g. T-67's if/else), the print went with it; nothing else was stripped.
- **T-40 DHub structural items** — missing RepeatForCopies support and the DGetParamRaw
  name-mangling tier need the tracked rewrite, not point fixes.
- **T-90** (hard-coded stack depths), **T-91** (general `_script` hazard — only the concrete
  DRenameItem/DTrigger manifestations were fixed), **T-92** (Copies > 9).
- All review "Cleanup", "Incomplete" and "Suggestions" items not required by a confirmed bug.
- Known limitation left in `>`: interior empty fields (`>>`) are still collapsed by `split()`;
  only the leading token was restored.

## Open follow-ups

1. **DromEd verification** of everything here (`script_reload`, `script_test`), starting with the
   four ⚠ commits above. No fix in this branch has run.
2. **Update `docs/OPEN_TASKS.md`** — the T-IDs in the table above are fixed here but their rows
   still say open. Flip them (or mark "fixed pending verification") when this branch merges.
3. **⚠ Main-checkout warning:** the primary working tree's *uncommitted* `DScript Core.nut`
   (debug-print removals) is re-encoded to UTF-8 **with the `§`/`°` bytes already destroyed
   (U+FFFD)** — including the functional `case '§':` label. That version must NOT be committed
   as-is; rebuild those edits on a clean ISO-8859-1 base (see `tools/dsedit.py` and the CLAUDE.md
   encoding section).
4. **Structural work skipped by design:** T-40 DHub rewrite, T-90 stack depths, T-91 general
   `_script` hazard, T-92 Copies > 9.
5. **Review backlog:** all Cleanup / Incomplete / Suggestions entries in the wave reports remain
   open; `DScript_ModdingTools.nut` is still entirely unreviewed (skipped mid-run in wave 2).
   The undercover suite's *tracked* bugs (T-16/T-31/T-41/T-42) were fixed in the 2026-08-13
   follow-up pass, but the suite still has no line-by-line review.
6. **`>` operator:** interior empty fields (`>>`) are still collapsed by `split()`.

## Notes for the merge

- `DScript Core.nut` stayed ISO-8859-1 throughout (all edits byte-safe); other files keep
  their existing UTF-8/ASCII encodings.
- `OPEN_TASKS.md` rows for the closed T-IDs still say open — update after DromEd verification.
- The wave-1/2 SUMMARY line numbers no longer match this branch (edits shifted lines);
  the byte-exact anchors still locate everything.
